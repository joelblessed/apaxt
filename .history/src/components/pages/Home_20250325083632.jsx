import React from 'react'

const Home = ({productsBySeller}) => {
  return (
    <div>
      {productsBySeller.map((products,index) =>(
        <</>
      ))}
      
    </div>
  )
}

export default Home
